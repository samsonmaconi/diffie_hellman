INSTALLATION AND EXECUTION INSTRUCTIONS
---------------------------------------

1. Run `npm install` in the project directory to install necessary node_modules for the app
2. Run `npm start` to execute the app and use on the console
